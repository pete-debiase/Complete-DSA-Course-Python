#!/usr/bin/env python3

# Which of the following are equivalent to O(n)? Why?

# 1. O(n + p), where p < n/2 ⇒ O(n)
# 2. O(2n)                   ⇒ O(n)
# 3. O(n + log n)            ⇒ O(n)
# 4. O(n + n log n)          ⇒ O(n log n)
# 5. O(n + m)                ⇒ O(n + m)
